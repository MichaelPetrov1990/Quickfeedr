   document.querySelector(".applicant_show").parentElement.insertAdjacentHTML("afterend", `
    <ul>
    <% @job_applications.each do |app| %>
        <%= link_to app.user.email, profile_path %>
    <%end%>
    </ul>
`);

